name: sms_auto_sender
description: Flutter version of SMS Auto Sender
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

  # Latest compatible versions (tested with Flutter 3.24+)
  firebase_core: ^4.2.1
  cloud_firestore: ^6.1.0
  sms_advanced: ^1.1.0
  permission_handler: ^12.0.1
  shared_preferences: ^2.3.2        # ← Fixed: 2.5.5 doesn't exist yet
  flutter_foreground_task: ^9.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true